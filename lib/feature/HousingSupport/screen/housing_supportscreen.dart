import 'package:flutter/material.dart';

class HousingSupportScreen extends StatelessWidget {
  const HousingSupportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('دعم سكني')));
  }
}
